project OCPP_CSMS is
   for Source_Dirs use ("src", "src/ocpp");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Compiler is
      for Default_Switches ("Ada") use ("-gnat2022", "-O0", "-g");
   end Compiler;
end OCPP_CSMS;
